Prerequisites
Install jsdom to enable DOM manipulation in Node.js:

bash
Copier le code
npm install jsdom




Usage
Save this script as cleanHTML.js.

Update filePath with the path to your HTML file.

Replace classNameToRemove with the class of elements you want to remove.

Run the script using Node.js:

bash
Copier le code
node cleanHTML.js